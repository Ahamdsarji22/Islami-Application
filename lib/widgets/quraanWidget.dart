import 'package:flutter/material.dart';

class quranWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('quran', style: TextStyle(fontSize: 30)),
    );
  }
}
