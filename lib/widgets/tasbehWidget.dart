import 'package:flutter/material.dart';

class tasbehWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('tasbeh', style: TextStyle(fontSize: 30)),
    );
  }
}
